name: hassaniya_translation
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  cloud_firestore: ^5.6.11
  firebase_storage: ^12.4.9
  firebase_core: ^3.15.1
  file_picker: ^10.2.0
  record: ^6.0.0
  path_provider: ^2.0.0
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/mots.csv
